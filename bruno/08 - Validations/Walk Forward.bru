meta {
  name: Run walk-forward validation
  type: http
  seq: 4
}
post {
  url: {{baseUrl}}/api/v1/experiment-runs/{{runId}}/validations/walk-forward
  body: json
  auth: none
}
body:json {
  {"mode":"expanding","training_window":504,"test_window":126,"step":126}
}
script:post-response {
  if (res.status === 201) bru.setEnvVar("walkForwardValidationId", res.body.id);
}
