meta {
  name: Health
  type: http
  seq: 1
}
get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}
tests {
  test("healthy", function () { expect(res.status).to.equal(200); expect(res.body.status).to.equal("ok"); });
}
