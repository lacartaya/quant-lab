meta {
  name: Verify Alpaca PAPER connectivity
  type: http
  seq: 1
}
get {
  url: {{baseUrl}}/api/v1/brokers/alpaca/paper/connectivity
  body: none
  auth: none
}
tests {
  test("paper connected", function () { expect(res.status).to.equal(200); expect(res.body.simulated).to.equal(true); });
}
