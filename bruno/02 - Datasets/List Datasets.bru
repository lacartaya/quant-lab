meta {
  name: List DatasetSnapshots
  type: http
  seq: 1
}
get {
  url: {{baseUrl}}/api/v1/datasets?limit=50&offset=0
  body: none
  auth: none
}
tests {
  test("listed", function () { expect(res.status).to.equal(200); });
}
